name: "CI Flow: Push Summary & MR Review"

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    types: [opened, synchronize]

jobs:
  # === 任務一：Push 時自動生成實作說明 ===
  push-summary:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Generate Summary Report
        run: |
          echo "## 📊 📊 程式碼提交自動說明報告" > report.md
          echo "* **提交人員:** ${{ github.actor }}" >> report.md
          echo "* **提交訊息 (實作內容):** $(git log -1 --pretty=format:'%s')" >> report.md
          
          echo "### 📂 本次修改的檔案清單：" >> report.md
          echo "\`\`\`diff" >> report.md
          git diff --name-status HEAD~1 HEAD >> report.md
          echo "\`\`\`" >> report.md

          cat report.md >> $GITHUB_STEP_SUMMARY

  # === 任務二：Merge Request AI 審查 ===
  mr-ai-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    # 💡 沿用你網頁上設定的 apikey 環境
    environment: apikey
    permissions:
      pull-requests: write
      contents: read

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch base branch for diff
        run: |
          git fetch origin ${{ github.event.pull_request.base.ref }}

      - name: Run AI Review
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
          PYTHON_CODE: |
            import os
            import requests

            def main():
                groq_key = os.environ.get('GROQ_API_KEY')
                github_token = os.environ.get('GITHUB_TOKEN')
                repo = os.environ.get('REPO')
                pr_num = os.environ.get('PR_NUMBER')

                if not os.path.exists('patch.diff'):
                    print("找不到 patch.diff")
                    return

                with open('patch.diff', 'r', encoding='utf-8') as f:
                    diff = f.read()

                if not diff.strip():
                    print("Diff 內容為空，跳過")
                    return

                # 💡 稍微放寬截斷線至 30,000 字元，確保 AI 能看到足夠的上下文
                if len(diff) > 30000:
                    diff = diff[:30000] + "\n\n...(內容過長已自動截斷，以符合 Groq 免費額度限制)..."

                prompt = f"""請擔任嚴格且精準的資深 Java/Spring Boot 工程師，進行 Code Review。

                【🚨 絕對禁止的行為與懲罰機制 (Critical Rules)】
                1. 嚴禁幻覺與找碴：如果程式碼已經在 `finally` 區塊中安全關閉資源，或者已經有註解如 `@PostConstruct`、Null 檢查等防呆機制，你卻指責它沒有處理，這是嚴重的幻覺！看清楚 Git Diff 的綠字(+)與白字(上下文)！
                2. 嚴禁空泛建議：任何在「必須修正」或「建議優化」提出的建議，**必須、絕對要附上具體的 Java 程式碼範例 (Code Snippet)**。如果給不出程式碼範例，就絕對不准提這個建議！
                3. 嚴禁湊字數：這是一份專業的 Review，如果沒有真正值得提的優化，請在「建議優化」果斷寫下「無」。不要為了顯得專業而給出無意義的建議。

                【Review 核心要求】
                1. 專注尋找：NullPointerException 風險、嚴重的效能問題 (N+1)、資源洩漏 (Resource Leak)、或是 Spring 依賴注入的潛在錯誤。
                2. 若程式碼品質良好，沒有明顯漏洞或需要優化的地方，請直接在「必須修正」與「建議優化」區塊填寫「無」。

                【回覆格式要求】
                ### 📝 MR 總結
                (2-3句話精簡說明)

                ### 🔴 必須修正 (Critical Issues)
                (若無請寫「無」。若有，必須說明邏輯錯誤並「附上修正 Code Snippet」)

                ### 🟡 建議優化 (Suggestions)
                (若無請寫「無」。若有，必須「附上優化 Code Snippet」，否則不准寫)

                ### 🟢 亮點與肯定 (Good Practices)
                (指出寫得好的防禦性設計)

                ---
                以下是 Git Diff 內容：
                ```diff
                {diff}
                ```"""
                headers = {'Authorization': f'Bearer {groq_key}', 'Content-Type': 'application/json'}
            
                # 雙模型防禦：首選現役最新的 3.3，備援 3.1
                models_to_try = ['llama-3.3-70b-versatile', 'llama-3.1-8b-instant']
                response = None
            
                for model in models_to_try:
                    print(f"正在嘗試呼叫 Groq API，使用模型: {model}...")
                    data = {'model': model, 'messages': [{'role': 'user', 'content': prompt}], 'temperature': 0.2}
                    try:
                        res = requests.post('https://api.groq.com/openai/v1/chat/completions', headers=headers, json=data)
                        if res.status_code == 200:
                            response = res
                            print(f"🎉 成功使用模型 {model} 取得回應！")
                            break
                        else:
                            print(f"❌ 模型 {model} 失敗，錯誤訊息: {res.text}")
                    except Exception as e:
                        print(f"💥 請求引發異常: {str(e)}")

                if not response:
                    print("🚨 抱歉，所有準備的 Groq 模型皆無法使用。")
                    return

                ai_reply = response.json()['choices'][0]['message']['content']
            
                print("正在留言到 GitHub...")
                gh_headers = {'Authorization': f'token {github_token}', 'Accept': 'application/vnd.github.v3+json'}
                gh_res = requests.post(f'https://api.github.com/repos/{repo}/issues/{pr_num}/comments', headers=gh_headers, json={'body': ai_reply})
                print(f"GitHub 回應狀態碼: {gh_res.status_code}")

            if __name__ == '__main__':
                main()
        run: |
          git diff origin/${{ github.event.pull_request.base.ref }} HEAD -- . ':(exclude)*.json' ':(exclude)*.lock' ':(exclude).github/*' > patch.diff
          echo "$PYTHON_CODE" > review.py
          python3 review.py